Todo list.  Open items only - finished work is in CHANGELOG.md and in the commit
messages that made the changes, not kept here.  Checks that need a person with a
device are in MANUAL_CHECKS.md, one line each here.  WIP.md holds the
longer-form handoff notes.

Cites name a symbol or a greppable string, not a file:line; a line number reads
as evidence while carrying none.  Each item is tagged with what the machine
picking it up has to have.

Two rounds ran on 2026-08-29.  The first had an SDK, an NDK and an API 36
emulator, and its results are in CHANGELOG.md's Verified section.  The second
had neither, and closed the four code items that headed this file - the C++
shim's drift from the shared original, R8's dishonest "Reading:" line, and the
terminal's two gaps.  They are gone from here and are in CHANGELOG.md.

What that second round could not do is watch any of it run, which is why
MANUAL_CHECKS.md gained a section and carries the sightings this file cannot.


- [ANY] the bundled offline help under app/src/main/assets/help/ is checked by
nothing.  romwbw_disks publishes the seven topics and ioscpm has
tools/check-help-assets.py for its own copy; there is no Android equivalent, so a
rewritten topic leaves this copy stale silently.  The seven files match
romwbw_disks/help/ as of 2026-09-18 - that was measured, not assumed.
  Refiled from romwbw_disks 2026-09-18: its CLAUDE.md names the gap and the check
belongs beside the copy it guards.

- [ANY] Nothing here ever tells the user that a disk and the selected RomWBW
release disagree, and the call that could is present and unused.
romwbwReleaseOfImage is exposed across the JNI boundary and has NO Kotlin caller
anywhere in the tree; v0NameOf goes one way only and hardcodes V0_LEGACY_ROMWBW,
so there is no inverse of z80cpmw's releaseOfV0Name to read a release back out of
a filename.  There is also no reconciliation or back-fill of any kind.
  Two more doors that are open and unused, both since the release filter went on
2026-09-17.  The index's hbios.ver_byte and hbios.upd_byte are still parsed into
RomwbwVersion and are still the ROM-to-disk-image pairing - they just stopped
being an emulator gate - so they are what a "this image is 3.5.1 and you are
running 3.6.0" notice would compare against; DiskDownloadManager.fetchAndReadRom
already uses them that way for the ROM half, as RomFailure.WrongRelease.  And
emu_romwbw_release_loaded() (romwbw_emu src/emu_init.h) reports the release of
the ROM actually in bank 0, which is the one honest answer to "what is this
machine running" and which nothing here asks: it would need a new JNI export, so
it lands on the same commit as the Kotlin declaration and the JniNameParityTest
list.  MainActivity's About dialog is where its answer belongs; it names the
SELECTED release today and says so in a comment.
  The ordinary paths do keep slots and the selection together - scope() and
diskSlotKey() suffix every slot with the stored release, so switching releases
parks the old slots rather than mixing them.  But "always" is too strong, and the
counter-example is worth writing down because it is a race and not a typo:
MainActivity.downloadDefaultDisk runs loadSelectedCatalog, which MAY write a new
selection, then suspends for a 49 MB download, then calls setDiskSlot, which
resolves the scope suffix at WRITE time - so the slot can land under a release
the download did not come from.  Resolving at write time is exactly the mechanism
that lets it cross.
  A second thing found while reading and not previously written down:
loadSelectedCatalog's fallback arm overwrites a stored preference.  When the
index no longer offers the preferred release, selectRomwbwVersion returns another
entry and setSelectedRomwbwVersion writes it over the user's choice.  That may
well be right - it is what keeps a machine bootable when a release is withdrawn -
but it is undocumented, and it means a user's choice is not durable.
  Filed because z80cpmw MEASURED the user-visible end of this family on
2026-09-17: 3.6.0 stored against two -v0-3.5.1 disks, reported by a start-time
notice that names no remedy.  This port would not even print the notice.
  One correction for whoever takes it: applyRomwbwVersion fetches nothing and
verifies nothing.  It writes the selection, re-reads settings, refreshes three
displays and shows a Toast.  The fetch-and-verify lives in downloadRomThenSwitch
and fetchRomForRelease, which call it only on success - so a fix that "just calls
applyRomwbwVersion" switches the machine to a release it has no ROM for.


NOBODY HAS SEEN THE NEW TERMINAL RUN
-------------------------------------

- [ANY, NEEDS NETWORK] 1.31 / versionCode 33 WENT INTO PLAY REVIEW ON
2026-09-10 and nothing in this tree may say it shipped until Play serves it.
Uploaded is not released, released is not rolled out, and rolled out is not
installed.  The measurement is the only thing allowed to move the record:

    sh tools/check-store-version.sh

While it still reports 1.25 there is nothing to do.  When it reports 1.31, this
file loses this item and CHANGELOG.md's 1.31 entry stops saying Play has not
seen the number.  Nothing else is owed, and there used to be: the script
compared its measurement against a shipped: field in another repository's
document, which is gone as of 2026-09-13 along with every reader of it.  The
script says so where the comparison used to stand - grep it for "is NOT
consulted".  The reading is the whole answer and the date it was taken is the
whole of its authority.

Do NOT pre-emptively write 33 into anything that records what users have, to
save a trip.  That is the exact failure the script was built to catch, in
z80cpmw, where a changelog was wrong by two releases and had already sent a
re-read to the wrong commit.

- [ANDROID DEVICE or EMULATOR] the 2026-08-29 terminal round added VT52, a
scrolling region, DECSC/DECRC, the query replies, seven editing commands and
per-cell bold, underline, blink and reverse - and none of it has been drawn on a
screen.  What it did get is not nothing and is worth knowing before repeating
it: the whole of emu_io_android.cpp compiles under host clang++ at -Wall
-Wextra against the siblings' real headers, TerminalView.kt and
EmulatorEngine.kt compile under kotlinc against real Android framework classes
(org.robolectric:android-all, API 36), and android_host_path_basename() was run
against the shared original for 403,760 differential cases with no mismatch.
None of that is an APK.  MANUAL_CHECKS.md section 5 lists what to point at it,
and the four-face paint path and the blink tick are the two that most need a
real GPU rather than an argument.


NEEDS THE TABLET, NOT AN EMULATOR
----------------------------------

- [ANDROID DEVICE] the keyboard-aware work (690da30) has now been watched, but
on a stock API 36 AVD.  The tablet is a different question and MANUAL_CHECKS.md
keeps it: Samsung SM-X200, Android 14, with Smart Keyboard Pro as the IME.  The
AVD's keyboard is not evidence about a third-party one.  Insets were never the
problem there - ime.bottom=636 was measured in July - so what is unsettled is
the drawing, and the drawing has now changed twice: a background colour was
added under it, and then a second paint path beside it.

- [ANDROID DEVICE] four questions only a hand can answer, listed in
MANUAL_CHECKS.md and unchanged by either round: which way a drag should scroll,
how many lines it should move, how much blank space should sit above the prompt
on a fresh boot with empty history, and whether a drag should scroll at all
while the keyboard is up.  A fresh boot does show a large blank band above the
prompt on an AVD, which is the live screen anchored at the bottom with nothing
in history yet - by design, and still worth a person's opinion.


BEFORE ANYONE ADDS DISK-IMAGE HASH VERIFICATION, READ THIS
-------------------------------------------------------------------------------

- [ANY] no disk image already on the device is ever hashed.  The catalog
document's sha256 is verified, and a download's bytes are verified as they
arrive, but nothing re-reads an installed file: isDiskDownloaded() decides by
filename.  That is
why the v0 migration needed no freshness work here, and it is also a trap for
whoever adds image verification later.
  hd1k_combo is the one image of twenty whose bytes differ between the pre-v0
catalog and catalog-v0-3.5.1.json, and the migration renames rather than
re-downloads, so every migrated device holds it under a hash the catalog does
not name.  It is not corrupt: the two images were compared byte for byte on
2026-09-05 and slice 0's directory lists the same 94 files, all 94 extract
byte-identical including r8.com and w8.com, slices 1-5 do not differ at all, and
the 2,342 bytes that do differ are CP/M slack between the files.  Two toolchains
wrote the same disk.
  So a naive "hash mismatch means re-download" would hand every migrated user a
49 MB download for nothing.  ioscpm and z80cpmw both carry the fix already -
CatalogMigration.equivalentPriorImage and diskv0::isEquivalentPriorImage, keyed
on provenance so that only the migration can produce it and a fresh download can
never be blessed by it.  Copy that shape rather than inventing one.
  romwbw_disks docs/FINDINGS.md section 10 has the measurements.

NOBODY HAS UPGRADED A DEVICE ACROSS THE V0 MIGRATION, OR SEEN THE CATALOG FETCH
-------------------------------------------------------------------------------

- [ANDROID DEVICE or EMULATOR] the v0 disk-name migration renames the user's
downloaded images, the copies they have written to from inside CP/M, and the
four disk_slot_N preferences that name them, and it has never run anywhere but
in an argument.  V0MigrationTest covers the name mapping and both directories
against temporary directories on a JVM, and those 14 tests now pass under the
host recipe in CHANGELOG.md's 1.27 preamble - not under Gradle, which still
cannot start here.  What no harness can settle is an upgrade in place over real
pre-v0 state, external storage that is not there, and a restore from backup with
allowBackup on: MANUAL_CHECKS.md section 6 has all three, and says to build the
old state first, because a fresh install has nothing to migrate and passes every
one of them for free.

- [ANDROID DEVICE or EMULATOR] releases B and C - the two-level catalog and the
RomWBW release picker - have never fetched anything.  What has been done is one
step further than 1.26 managed and worth knowing before repeating it: every
Kotlin source in the app type-checks under kotlinc against real android.*,
androidx, okhttp and coroutines classes with only R, BuildConfig and
ActivitySettingsBinding stubbed, and every JVM suite runs and passes.
CHANGELOG.md's 1.28 preamble has the exact recipe; it needs no SDK.  What none
of it reaches is a device.  MANUAL_CHECKS.md section 7 has the six that need
one, and its check 2 is the one to do first.  A Kotlin/C++ NAME mismatch in the
three JNI bindings added in 1.27 does now have a guard - JniNameParityTest reads
both source files as text and compares the lists in each direction, 35 names on
each side, 3 tests, recorded as run and passing in CHANGELOG.md's 1.27 entry -
but it is opt-in: it runs under `./gradlew :app:test`, README's build steps say
assembleDebug, and there is no CI, so an ordinary build of this tree still
catches nothing.  Past the name, nothing is guarded at all: per CHANGELOG.md's
1.27 entry the JNI translation unit has only been -fsyntax-only type-checked -
no object file, no .so, and no linker has ever looked for these symbols - so a
descriptor/signature mismatch, or a symbol the NDK never emitted, is still an
UnsatisfiedLinkError the first time Settings is opened and nowhere earlier.

- [ANDROID] DISK_NAMES_MIGRATION_PASS is 2 because release A shipped a flag
saying the rename was done while this build still downloaded pre-v0 names, so a
disk fetched under 1.26 could arrive after it.  Pass 2 sweeps those up.  There
should never be a pass 3: after this release the catalog publishes only v0
names.  If one is ever needed, that is evidence something started writing
pre-v0 names again, and the reason matters more than the sweep.

- [ANDROID DEVICE or EMULATOR] the ROM now comes from the catalog, the user
picks which one, and nothing about either has been watched on a screen.  What
was run is in CHANGELOG.md: every Kotlin source type-checks, and the four JVM
suites - catalog parsing, ROM selection, the v0 migration and JNI name parity -
pass.  None of them reads a file, opens a socket or boots a guest.
MANUAL_CHECKS.md section 8 has twelve that need a device, and three are worth
doing before the others.  Check 1, that Start is pressable on an install with no
ROM: z80cpmw shipped exactly that bug - updateMenuState still required hasROM()
after its ROMs were deleted, so Start was greyed on every fresh install and
Start is the only thing that fetches a ROM - and a person installing the package
found it, because the scripted driver posted the command straight to the window
and never looked at the gating.  Check 2, that a first launch with no network
names the release and the reason instead of hanging, which is the cost this
change deliberately accepts.  And check 9, that a corrupt ROM stops the machine:
there is no bundled ROM left for it to fall back to, so what that check now
watches for is not a silent fallback but a silent boot on anything at all.

- [ANY] check-store-version.sh maps Play's versionName to a versionCode through
CHANGELOG.md headings, so it cannot see a build newer than this tree - the listing
carries no versionCode.  ioscpm hit exactly this on 2026-09-13: the Mac Store was
serving build 72 while that tree and its origin topped out at 70, and its script had
reported "at most build 70" with no way to know otherwise.  Same shape here.

- [ANDROID] an image downloaded before the v1.4.12 repin keeps the destructive R8 and
there is no way out of it from inside the app.  That r8.com copies the host basename
into the FCB unfiltered, so '?' and '*' make it ambiguous and F_DELETE runs before
F_MAKE: importing a?b.txt erases every CP/M file matching A?B.TXT first and says
nothing (romwbw_emu CHANGELOG, "R8 erased unrelated CP/M files" - verified live, two
unrelated files went).  Downloads only happen for an ABSENT file, so the repin does
not refresh what is already there.  This port has no DiskLedger and no
refreshSuperseded, and deleteDisk()/deletePersistedDisk() in DiskDownloadManager.kt
have no caller anywhere under app/ - the Settings X is clearDiskSlot(), which sets the
slot to null and leaves the file on the device.  Reinstalling is the only remedy a
user has.  ioscpm solves this with a ledger keyed on provenance - the catalog sha256
a verified download matched - not on hashing the installed file, because a downloaded
disk is a writable volume the guest rewrites and that comparison calls every disk a
user has saved work into stale.  DiskLedger.action is the shape: refresh unasked only
when superseded AND pristine, offer when it would be lossy, stand down while the
emulator holds the file open.  Copy that rather than inventing one.

- [ANDROID] a ROM is never deleted, including a bad one.  A file that fails
verification is re-downloaded once, and the fetch renames a fully verified
transfer over it - so a failed attempt leaves what was there rather than leaving
nothing, and a ROM a user put in that directory themselves is never removed.
The cost is an unused 512 KB file per withdrawn release, which nothing reclaims.
That is the deliberate side of the trade; if it ever becomes a complaint, the
answer is a user-initiated cleanup, not an automatic one.


THE PACKAGE IS BEHIND THE TREE
-------------------------------

- [ANDROID] three HBIOS paths this port implements have never been executed on
a device: HBF_HOST_CAPS, HBF_HOST_GETNAME and HBF_HOST_GETRNAME.  The procedure
is a keyboard-and-screen one and lives in MANUAL_CHECKS.md now, under "The three
HBIOS host-file paths nothing has ever run"; what stays here is only that it is
outstanding.


TERMINAL: WHAT IS STILL NOT THERE
----------------------------------

The parser gained VT52, DECSTBM, DECSC/DECRC, the query replies, ICH/DCH/ECH/
IL/DL/SU/SD, CHA/HPA/VPA, the three private modes, deferred autowrap and
per-cell attributes.  What is left is shorter than what went, and most of it is
absent from the siblings too - which is worth saying, because a gap all three
ports share is a different kind of item from one only this port has.

- [ANDROID] this port prints every byte from 0x20 up, where both siblings stop
at 0x7E.  So 0x7F (DEL) draws a glyph here and nothing there, and so does every
byte from 0x80 to 0xFF - which a CP/M program using the high half of a code page
will emit, and which arrives as whatever the JVM makes of the Char.  This
predates the terminal round and was found during its review; it is the last
divergence in the normal state.

- [ALL PORTS] none of the three has ENQ (0x05) answerback, CNL (CSI E), CPL
(CSI F), REP (CSI b), TBC/HTS, CBT (CSI Z), CHT (CSI I), origin mode (DECOM,
?6), an alternate screen buffer, left/right margins, a tab-stop table, charset
remapping, or OSC/DCS strings.  FF (0x0C) belongs on this list too: it is
discarded here, and neither sibling has a case for it either.  Nothing has
asked for any of them.  Listed so the next cross-port sweep can see they were
considered rather than missed.

- [DELIBERATE, do not report this as a gap again] SGR 0 resets the foreground
to Color.GREEN here, where ioscpm's applySGR and z80cpmw's both reset to CGA 7
light grey.  The green is this app's identity and the owner confirmed keeping
it on 2026-08-29.  DEFAULT_FG and DEFAULT_BG in TerminalView.kt carry the same
note.  A cross-port sweep will keep finding this; it is a divergence that was
chosen.

- [DELIBERATE] ESC[104m is light blue here and normal blue in z80cpmw.  That
port packs a cell into a CGA attribute byte whose background field borders the
blink bit and so folds 100-107 onto 40-47; a cell here is a full ARGB Int with
nothing to borrow, so the bright backgrounds stay bright.  Same asymmetry the
bright foreground range already had.

- [DELIBERATE] SGR 1 does not brighten the foreground here.  z80cpmw's bold
sets CGA bit 3, because in a packed attribute byte bold and bright ARE the same
bit and cannot be separated.  A cell here carries a full ARGB foreground and a
CELL_BOLD flag beside it, so ESC[1m picks the heavy face and leaves the colour
alone - which is what makes ESC[22m undo exactly what ESC[1m did.

- [DELIBERATE] SGR 39 and 49 work here and in neither sibling.  This is the one
divergence in the SGR handler that makes this port MORE conformant, not less,
and it predates the attribute work.


SCROLLBACK IS ROW 2, AND ROW 2 IS NOT LEVEL
--------------------------------------------

Found on 2026-09-02 from the other side: ioscpm's scrollback turned out never to
have captured a line in its life - its line-feed handler called scrollRegion(),
which shifts rows without keeping them, where only scrollUp() appends to the
buffer.  It was fixed there by moving the whole-screen test inside
scrollRegion() so no call site can miss it.  Reading the three ports side by
side to write that up turned up four things here.  None of them was found by a
device; all four are visible in this tree with grep.

Two of the four are closed: the snap-to-live and the keyboard-up rendering were
fixed on 2026-09-02 and are in CHANGELOG.md.  What is left is one deliberate
divergence and one small drawing bug.

- [DELIBERATE, corrected 2026-09-02] a cold boot keeps the previous session's
history, and this was written up here as a defect on the day it was found.  That
was wrong, and clear()'s own docstring says why: "The scrollback itself is
deliberately kept... losing the user's history is a product decision, not part of
putting the terminal back to power-on."  It even names how the siblings differ -
both drop it at the CALL SITE, z80cpmw's startEmulator and onEmulatorReset
calling resetScrollback beside clear(), ioscpm's reset() emptying its buffer -
which here would be MainActivity.  So the divergence is real and intentional, and
FEATURE_PARITY.md item 2's "history cleared on emulator start/reset" is a spec
line the three ports disagree on by choice rather than one of them being broken.
Left here as a decision someone may still want to revisit at the MainActivity
call site, not as a bug.  Worth noting how it was got wrong: the code said so in
a comment nobody read, which is the same failure the parity document made.

- [ANDROID DEVICE] the scrollback fixes of 2026-09-02 have not been watched
run.  There is no SDK, no kotlinc and no emulator on the machine that made them,
so what backs them is a brace-balance check, a line-by-line read against both
siblings, and a port of onDraw's windowing arithmetic to a scratch script that
reproduces the old keyboard-down behaviour exactly and shows the new keyboard-up
window reaching the top of the live screen.  None of that is a screen.  What to
point at it: drag back with the keyboard closed, then open the keyboard and drag
again; run a long DIR and confirm the view stays put while it prints; type a key
and confirm it returns to live; confirm the cursor is absent while scrolled back
and present at the bottom.


- [ANDROID] the catalog index is switchable in the model but has no UI yet.
SettingsRepository now carries KEY_CATALOG_INDEX_URL (empty = the index this
build ships with), resolveIndexUrl/isCustomIndex/fnv1a32, and the instance
accessors catalogIndexUrl, effectiveIndexUrl, usingCustomIndex and
indexUrlIsFromEnvironment.  $ROMWBW_INDEX_URL wins over the setting for one run,
which is what a test uses and what works TODAY with no UI:

    adb shell setprop ... is not it - use the run configuration's environment,
    or set the pref directly while debugging.

  Everything a switch has to move moves with it.  scope() appends indexScope to
every per-release key - disk slots, NVRAM, catalog generation, the ROM claim -
and DiskDownloadManager's disksDirName()/modifiedDisksDirName() append it to the
folders, so a visit to a test catalog cannot read or write the library the device
already has, and coming back finds it untouched.  indexScope is EMPTY for the
built-in index, so every existing install's keys and folders are byte-identical
to what they were.
  WHAT IS MISSING is a field in SettingsActivity: a text box bound to
catalogIndexUrl with "built-in catalog" as its hint, disabled when
indexUrlIsFromEnvironment, a line showing effectiveIndexUrl, and a warning when
usingCustomIndex saying the catalog is the thing being trusted - downloads are
still checked against that catalog's own sha256, but the index is the root of
that chain.  Applying one must call refreshIndexScope() (the setter already does)
and then re-fetch the catalog.  ioscpm's Settings "Catalog" section is the model
to copy; z80cpmw's SettingsDialogWx has the same field.
  NONE OF THIS HAS BEEN COMPILED.  It was written on a machine with no Android
SDK, no Gradle and no JRE - /usr/bin/java is the Apple stub - so not one line of
it has been through kotlinc.  Read it before running it.

- [ANDROID] the manifest declares two storage permissions the app does not use.
app/src/main/AndroidManifest.xml:5-7 declares READ_EXTERNAL_STORAGE with no
maxSdkVersion and WRITE_EXTERNAL_STORAGE capped at 28.  nothing reads or writes
shared storage: grep for getExternalStorage / Environment.getExternal across
app/src/main/java finds nothing, the Imports and Exports folders are
getExternalFilesDir(null) (HostTransfer.kt:76), which has needed no permission
since API 19 and minSdk here is 24, and both the import picker and the share
target go through the system.
  WHY IT MATTERS is not the grant, it is the listing.  Play shows a user what an
app DECLARES, so the store page offers storage permissions for an app whose
privacy policy said "Permissions used: Internet access" - the policy now
describes them instead, which is honest but is not the fix.  READ_EXTERNAL_STORAGE
is also the permission the Data Collection section promises is not used to reach
"your contacts, photos, or other personal files".
  DELETE BOTH LINES and ship it.  it needs a build, an emulator run of the file
transfer path in both directions, and a versionCode bump, which is why it is
here and not done - none of that can happen on a machine with no Android SDK.
NOT VERIFIED AGAINST A MERGED MANIFEST: a dependency could in principle
re-introduce either, and only a Gradle build prints the merged result.

- [ANDROID DEVICE] a release switch fills no empty disk slot, and the code that
was written to do it could not have.  fillEmptySlotsWithDefaults() and
DEFAULT_DISK_IDS were written on 2026-09-18 and are NOT in the tree, deliberately:
the helper reads cachedSelection and applyRomwbwVersion clears it one line before
calling, so it returned an empty list on every call and no slot was ever filled -
and reordering those two lines does not fix it.  Every write of cachedSelection
comes from loadSelectedCatalog(), which loads the catalog of the release that is
selected NOW; applyRomwbwVersion sets the new release first, so the guard
comparing the cached selection against the release being switched TO can only
match if the new release's catalog has already been fetched.  Doing this properly
means an async catalog load on the switch path, which is the work.
  Then the question that was left open before any of it: a switch that fetches
what is missing spends 49 MB (hd1k_combo) or 57 MB with hd1k_games, measured
against the 3.6.0 catalog, and this app has no metered-connection awareness at
all - no ConnectivityManager, no isActiveNetworkMetered - while showDownloadConfirmation
already asks before fetching a single disk.  z80cpmw downloads silently.  Ask
once naming the size is the house style here.
  THE FOUR RULES THE DELETED HELPER GOT RIGHT, written down because the code is
gone and these are the part worth keeping.  (1) Name the disks BY CATALOG ID -
hd1k_combo then hd1k_games - and never by filename: the filename is the part that
moves, hd1k_combo-v0-3.5.1.img becoming hd1k_combo-v0-3.7.0-dev.14.img the moment
the release changes, and CATALOG_SCHEMA.md 6.1 asks a client to key on the id for
exactly that reason.  Both ids exist in all three published releases, measured
2026-09-18.  (2) Fill EMPTY slots only; an occupied slot is left exactly as it is,
which is the rule a version switch must never break.  (3) An image that is not
downloaded is NAMED, not fetched - that is what the question above is about.
(4) The catalog nominates no default DISK to read this from: a roms[] entry
carries default:true, a disks[] entry carries no equivalent in the schema or in
any published document, so the list is this application's own and has to be
written down rather than derived.  See the next item for the field that looks
like it would answer this and does not.

- [ANY] the About dialog and createReleaseMismatchNotice() both name the SELECTED
release, and neither asks what is actually in bank 0.  emu_romwbw_release_loaded()
(romwbw_emu src/emu_init.h) reports the release of the ROM the machine booted,
which is the one honest answer to "what is this machine running" and the only one
that would catch a ROM that verified against the wrong release.  It needs a new
JNI export, so it lands on the same commit as the Kotlin declaration and the
JniNameParityTest list.  A pre-release cannot be named from it either way: the
two HCB bytes spell 3.7.0 for a 3.7.0-dev.14 ROM, so the suffix exists only in
the catalog string and in the v0 filename releaseOfV0Name() reads.

- [ANY] the first-launch path reads defaultSlot as "this is the starter disk",
and that is not what the field means.  MainActivity picks the disk with
catalog.disks.find { it.defaultSlot == 0 } and mounts it in slot 0.
romwbw_disks docs/CATALOG_SCHEMA.md 3.3 is explicit: defaultSlot "is the slice a
client should boot from when it mounts this image with no other instruction", an
index INSIDE hd1k_combo, and it says nothing about which disk is a starter or
which drive anything belongs in.  It exists only on hd1k_combo and its only
published value is 0, so the find lands on hd1k_combo - the right outcome for the
wrong reason.
  The failure this would produce is NOT the one the sibling has, and that is
worth knowing before fixing either: ioscpm reads the same field as a DRIVE number
and a published 3 would mount combo in drive 3 leaving drive 0 empty (filed in
that port's todo.txt, 2026-09-18).  Here a published value other than 0 - combo
gaining a different boot slice, which the schema permits since the field's whole
purpose is to name one - makes find return null, and a first launch then
nominates no starter disk at all and downloads nothing.  The comment above the
call already says "no entry carrying it is a catalog that simply nominates no
starter disk, not an error", which is true of an ABSENT field and would silently
become the behaviour for a PRESENT one.
  What it should key on instead is the id, hd1k_combo, the same way the item
above says to - not a slice index that happens to be zero.
